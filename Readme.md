## 🛠 Features
* **Static Typing:** Catch errors during development.
* **Ready-to-go `tsconfig.json`:** Optimized for Node.js (ES6+).
* **Dual Execution Modes:** Manual compilation (`tsc`) and instant execution (`ts-node`).

